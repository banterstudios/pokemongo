/* Import action types */
/* None yet. */


/* Initial state */
const initialState = {
	isLoggedin : false,
}

const userReducer = (state = initialState, action) => {

	switch (action.type) {
		
		default:
			return state;

   }

}

export default userReducer